class Bird(object):

  speed =
  placement =
  type =

  def _init_(self, speed, placement, type):
    self.speed = speed
    self.placement = placement
    self.type = type

def make_bird(speed, placement, type):
  bird = Bird(speed, placement, type)
  return bird
